<%= form_for([:admin,@city]) do |f| %>
  <% if @city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@city.errors.count, "error") %> prohibited this admin_city from being saved:</h2>

      <ul>
      <% @city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form">
    <%= f.label :state_id, 'Estado' %>
    <%= f.select :state_id, states_list(@states), { include_blank: @city.new_record? ? true : false } %>
  </div>
  <div class="form">
    <%= f.label :name, 'Nome' %>
    <%= f.text_field :name, cols: 70 %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
